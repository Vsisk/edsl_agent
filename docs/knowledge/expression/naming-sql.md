# Expression Naming SQL Rules

Naming SQL resources represent named query access to BO data.

- Use naming SQL only when the query or source node implies query-backed data access.
- Keep SQL parameters and condition fields together with the selected naming SQL.
- Merge SQL branch observations into selected resources without replacing unrelated context.
- Naming SQL selection should remain a resource-search concern, not a runtime concern.
