# Expression BO Rules

BO resources represent structured business objects.

- Select BOs by target semantics, query terms, and return type expectations.
- Include key fields when needed for navigation or identity.
- Include only relevant fields for generation unless validation requires broader metadata.
- Direct BO field mapping is a business route outside generic runtime control.
