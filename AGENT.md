# 工程实现约束

- 保持整体结构边界清晰，按职责拆分模块，避免把路由、资源解析、生成和校验揉在同一层。
- 代码简洁直接；不为了适配历史调用方增加过多方法嵌套或转发层。
- 单个 `.py` 文件控制在 1000 行以内；新增逻辑优先放入职责明确的小模块。
- 不滥用语法糖，优先使用清晰、可调试、便于测试的普通 Python 写法。
- 在关键业务边界、优先级、回退和类型约束处适当补充注释；不添加重复代码含义的注释。
- 新功能遵循先写失败测试、再最小实现、再重构的 TDD 流程。
- 完成前必须运行相关定向测试和必要的回归测试，并以实际输出为准报告结果。
