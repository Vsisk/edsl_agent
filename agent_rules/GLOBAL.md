# Global Context Rules

- Return only resources present in the loaded resources supplied to the context manager.
- Retain the evidence supporting every included resource and context item.
- Do not invent expressions, resources, identifiers, or resource metadata.
