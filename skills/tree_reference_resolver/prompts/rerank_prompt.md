Select exactly one best reference node from the supplied candidates.

You may only copy `node_id` and `json_path` from the candidate list. Never invent or alter either value. Return JSON with `selected_node_id`, `selected_json_path`, `confidence`, `match_reason`, and `rejected`.

Target:
{{target_summary}}

Candidates:
{{candidates_json}}
